/* import { TestBed } from '@angular/core/testing';

import { CongigService } from './config.service';

describe('CongigService', () => {
  let service: CongigService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(CongigService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
}); */
